# ngrok authtoken (copy to poc/ngrok.env — gitignored)
# Get yours: https://dashboard.ngrok.com/get-started/your-authtoken
export NGROK_AUTHTOKEN=""

# Local port for kappy-spike sms-reg-webhook (default 8790)
export KAPPY_SMS_REG_WEBHOOK_PORT="${KAPPY_SMS_REG_WEBHOOK_PORT:-8790}"
