# Line 1 — copy to poc/lines/line-1.env (gitignored; fleet test accounts OK)
# Line 1 uses existing spike/state

export KAPPY_SPIKE_STATE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)/spike/state"
export KAPPY_APPLE_ID="henry@jabronicapital.com"
export KAPPY_APPLE_PASSWORD=""
